export class Dna {
  name: string;
  dpi: string;
  dna: string;

  constructor(name: string, dpi: string, dnaString: string) {
    this.name = name;
    this.dpi = dpi;
    this.dna = dnaString;
  }
}
